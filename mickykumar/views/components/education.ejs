<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Terminal Education</title>
  <style>
    /* Terminal style */
    body {
      background-color: #0d0d0d;
      color: #33ff33;
      font-family: 'Courier New', Courier, monospace;
      margin: 0;
      padding: 2rem;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: start;
    }

    .terminal-container {
      background-color: #000000;
      border-radius: 8px;
      padding: 2rem;
      width: 90%;
      max-width: 800px;
      box-shadow: 0 0 15px #33ff33;
      overflow-y: auto;
      max-height: 90vh;
    }

    h1 {
      margin-top: 0;
      font-weight: bold;
      font-size: 2rem;
      border-bottom: 1px solid #33ff33;
      padding-bottom: 0.5rem;
    }

    .command {
      color: #00ffff;
      font-weight: bold;
      margin: 1.5rem 0 0.5rem 0;
    }

    .description {
      margin-left: 1rem;
      margin-bottom: 1rem;
      color: #a0ffa0;
    }

    .example {
      background-color: #001100;
      padding: 1rem;
      margin: 0.5rem 0 1rem 0;
      border-left: 4px solid #33ff33;
      white-space: pre-wrap;
      font-size: 0.95rem;
      color: #ccffcc;
      font-family: 'Courier New', Courier, monospace;
    }

    /* Scrollbar */
    .terminal-container::-webkit-scrollbar {
      width: 8px;
    }
    .terminal-container::-webkit-scrollbar-track {
      background: #000;
    }
    .terminal-container::-webkit-scrollbar-thumb {
      background-color: #33ff33;
      border-radius: 4px;
    }

  </style>
</head>
<body>
  <div class="terminal-container" role="main" aria-label="Terminal education content">
    <h1>Terminal Education</h1>

    <section>
      <div class="command">pwd</div>
      <div class="description">Prints the current working directory.</div>
      <div class="example">$ pwd
/home/user</div>
    </section>

    <section>
      <div class="command">ls</div>
      <div class="description">Lists files and directories in the current folder.</div>
      <div class="example">$ ls
Documents  Downloads  Pictures  terminal-education.html</div>
    </section>

    <section>
      <div class="command">cd &lt;directory&gt;</div>
      <div class="description">Changes the current directory to &lt;directory&gt;.</div>
      <div class="example">$ cd Documents
$ pwd
/home/user/Documents</div>
    </section>

    <section>
      <div class="command">mkdir &lt;folder-name&gt;</div>
      <div class="description">Creates a new folder named &lt;folder-name&gt;.</div>
      <div class="example">$ mkdir projects</div>
    </section>

    <section>
      <div class="command">touch &lt;file-name&gt;</div>
      <div class="description">Creates an empty file named &lt;file-name&gt;.</div>
      <div class="example">$ touch notes.txt</div>
    </section>

    <section>
      <div class="command">cat &lt;file-name&gt;</div>
      <div class="description">Displays the contents of the file &lt;file-name&gt;.</div>
      <div class="example">$ cat notes.txt
This is a sample note.</div>
    </section>

    <section>
      <div class="command">rm &lt;file-name&gt;</div>
      <div class="description">Deletes the specified file.</div>
      <div class="example">$ rm oldfile.txt</div>
    </section>

    <section>
      <div class="command">clear</div>
      <div class="description">Clears the terminal screen.</div>
      <div class="example">$ clear</div>
    </section>

    <section>
      <div class="command">man &lt;command&gt;</div>
      <div class="description">Shows the manual page for the specified command.</div>
      <div class="example">$ man ls</div>
    </section>
  </div>
</body>
</html>
